# numpy must be pinned before pandas to avoid binary incompatibility
# (pandas 2.0.x C extensions were compiled against numpy 1.x)
numpy==1.24.4
pandas==2.0.3
sqlalchemy==2.0.0
pymysql==1.1.0
# Required by PyMySQL to authenticate against MySQL 8.0's default
# caching_sha2_password auth plugin
cryptography==41.0.7